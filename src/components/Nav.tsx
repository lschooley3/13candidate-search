const Nav = () => {
  // TODO: Add necessary code to display the navigation bar and link between the pages
  return (
    <div>
      <nav className="nav">
        <a className="nav-link" href='/'>Candidate Search</a>
        <a className="nav-link" href='/savedCandidates'>Saved Candidates</a>
      </nav>
    </div>
  )
};

export default Nav;
